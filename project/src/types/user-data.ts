export type UserData = {
  id: number;
  email: string;
  token: string;
  name: string;
  avatarUrl: string;
  isPro: boolean;
};

export type UserDataProfile = {
  email: string;
  avatarUrl: string;
};
